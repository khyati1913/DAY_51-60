Q109: Write a program to take an integer array arr and an integer k as inputs. Print the maximum sum of all the subarrays of size k.

/*
Sample Test Cases:
Input 1:
arr[100, 200, 300, 400] = , k = 2
Output 1:
700

Input 2:
arr[1, 4, 2, 10, 23, 3, 1, 0, 20] = , k = 4
Output 2:
39

Input 3:
arr[100, 200, 300, 400] = , k = 1
Output 3:
400

*/

#include <stdio.h>

int main() {
    int nums[100], answer[100], n;
    int i, j;

    printf("Enter size of array: ");
    scanf("%d", &n);

    printf("Enter %d elements:\n", n);
    for(i = 0; i < n; i++) {
        scanf("%d", &nums[i]);
    }

    for(i = 0; i < n; i++) {
        int product = 1;
        for(j = 0; j < n; j++) {
            if(j != i) {
                product *= nums[j];   // multiply all except nums[i]
            }
        }
        answer[i] = product;
    }

    printf("[");
    for(i = 0; i < n; i++) {
        if(i < n - 1)
            printf("%d,", answer[i]);
        else
            printf("%d", answer[i]);
    }
    printf("]\n");

    return 0;
}
